import React from 'react';
import sendRating from '../../util/rating_api_util.js';

const Rating = ({card}) => {
  //
  // const rateCard = () => {
  //   sendRating(card.id);
  // };

  return <div>
    stars
  </div>;
};

export default Rating;
